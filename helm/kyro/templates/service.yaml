apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyro.fullname" . }}
  labels:
    {{- include "kyro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kyro.selectorLabels" . | nindent 4 }}
